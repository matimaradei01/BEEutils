import { Tabla } from "./tabla";
import { Contador } from "./contador";

export abstract class Constraint {
      
    abstract generarConstraint(tabla: Tabla, contador: Contador): string;
    abstract sumarContador(contador: Contador): Contador;
    
}