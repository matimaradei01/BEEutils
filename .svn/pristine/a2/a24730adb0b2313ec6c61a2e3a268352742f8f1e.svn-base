<div class="container mt-3">
<h1 class="display-4 mb-3">Generador de Grants</h1>
<div class="d-flex flex-column">
<form class="d-flex flex-column contenedor-formulario" [formGroup]="grantForm" (ngSubmit)="generarGrants()">
    
    <!-- <div class="form-group">
      <label for="esquema">Esquema</label>
      <select formControlName="esquema" class="form-control" name="esquema">
        <option *ngFor="let esquema of esquemas" [ngValue]="esquema">{{esquema}}</option>
      </select>
    </div> -->
    

    <mat-form-field>
      <mat-select formControlName="esquema" placeholder="Esquema" name="esquema">
        <mat-option *ngFor="let esquema of esquemas" value="esquema">{{esquema}}</mat-option>
      </mat-select>
      <mat-hint align="end">Seleccionar ^</mat-hint>
    </mat-form-field>


    <!-- <div class="form-group">
      <label for="tabla">Tabla</label>
      <input [ngClass]="{'is-invalid':nombreTabla.invalid && (nombreTabla.dirty || nombreTabla.touched)}" pattern="[a-zA-Z_]*" formControlName="nombreTabla" class="form-control" type="text" placeholder="Ingrese la tabla a generar" required>
      <div class="invalid-feedback">
          Dale.. Crack...
      </div>
    </div> -->

    <mat-form-field class="example-full-width">
      <input matInput placeholder="Tabla a generar" 
            [formControl]="nombreTabla"
            [ngClass]="{'is-invalid':nombreTabla.invalid && (nombreTabla.dirty || nombreTabla.touched)}" pattern="[a-zA-Z_]*">
      <mat-error *ngIf="nombreTabla.hasError('required')">
        Dale.. <strong>Crack...</strong>
      </mat-error>
    </mat-form-field>

    <!-- <div class="form-group">
      <label for="tipoTabla">Tipo de tabla</label>
      <select formControlName="tipoTabla" class="form-control" >
        <option *ngFor="let tipo of tiposDeTablas" [ngValue]="tipo" >{{tipo}}</option>
      </select>
    </div> -->

    <mat-form-field>
      <mat-select formControlName="tipoTabla" placeholder="Tipo de tabla" name="esquema">
        <mat-option *ngFor="let tipo of tiposDeTablas" value="tipo">{{tipo}}</mat-option>
      </mat-select>
      <mat-hint align="end">Seleccionar ^</mat-hint>
    </mat-form-field>

     <!-- <div class="form-group">
      <div class="custom-control custom-radio custom-control-inline" *ngFor="let app of aplicaciones">
        <input formControlName="aplicacion" type="radio" id="radio{{app.aplicacion}}" value="{{app.rol}}" class="custom-control-input">
        <label class="custom-control-label" for="radio{{app.aplicacion}}">{{app.aplicacion}}</label>
      </div>
    </div> -->
    <!--
      <p class="lead text-info leyenda">
          Recordá que lo generado por esta aplicación, es solo orientativo.
      </p>
    -->
    
      
    <mat-radio-group aria-label="Select an option" formControlName="aplicacion">
      <ng-container *ngFor="let app of aplicaciones">
        <mat-radio-button id="radio{{app.aplicacion}}" value="{{app.rol}}" color="primary">{{app.aplicacion}}</mat-radio-button>
      </ng-container>
    </mat-radio-group>
    
    <div class="alert alert-warning" role="alert">
      Recordá que lo generado por esta aplicación, es solo orientativo.
	  
    </div>

    <!-- <button type="submit" [disabled]="!grantForm.valid" class="btn btn-primary btnSubmit">Generar</button> -->

    <button mat-raised-button color="primary" type="submit" [disabled]="!grantForm.valid">Generar</button>
</form>
<div class="resultado mt-4">
  
  <router-outlet></router-outlet>
  
</div>
</div>
</div>